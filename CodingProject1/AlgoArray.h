#pragma once

#include "DynArray.h"

double sum(const DynamicArray &dynamicArray);

/**
 * @brief
 *
 * @param dynamicArry
 * @return double
 */
double mean(const DynamicArray &dynamicArry);

/**
 * @brief
 *
 * @param dynamicArry
 * @return double
 */
double median(const DynamicArray &dynamicArray);
